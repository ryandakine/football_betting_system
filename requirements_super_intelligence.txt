# Core ML Libraries
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.10.0

# Advanced ML
xgboost>=2.0.0

# Data processing
python-dotenv>=1.0.0
requests>=2.31.0

# Existing requirements (if not already installed)
# Add these if they're not in your main requirements.txt
# asyncio (built-in)
# pathlib (built-in)
# json (built-in)
# logging (built-in)
# pickle (built-in)
